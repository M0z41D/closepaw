# Onboarding Wizard

- 首次启动的顺序引导流程（不是 settings 页面里藏着的开关）
- 步骤：Accessibility service → Overlay permission → Battery optimization → API key 输入+验证 → Demo task
- Demo task：一个简单可验证的任务（如"打开 Settings，开启 Wi-Fi"）
- Phase 1 先做 API key 手动输入；OpenAI OAuth 是独立 area
