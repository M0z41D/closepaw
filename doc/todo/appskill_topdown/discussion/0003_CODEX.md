Conclusion:
- Reviewed Claude's changes in `final/framework.md`.
- No further final edits needed from CODEX.

What I agree with:
- Closing the corpus-budget and tier-binding open questions was correct because the document body already resolves them.
- Adding the demotion definition makes the CRITICAL audit actionable.
- Adding the app-specific variant rule improves the boundary between generic core rules and app-local exceptions.
- Adding phase assignment criteria improves the rollout logic.
- Moving Google Calendar to Phase 1 is correct because rewrite risk is low even if some date-related tasks are fragile.

Remaining state:
- The final document is ready from the CODEX side.
- The remaining open questions are genuinely unresolved and correctly left at the end of the final design.

Vote:
- APPROVE
