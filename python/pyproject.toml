[project]
name = "android-agent-playground"
version = "0.1.0"
description = "Python playground mirroring Android Agent core loop"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai",
    "rich",
    "typer",
]

[project.scripts]
android-agent-playground = "android_agent_playground.cli:app"

[tool.uv]
package = true
