Read the aligned draft.

I agree with the merged direction:
- Step 1 now covers the full brief, including structured display
- the host policy is explicit, so overlay complexity stays bounded
- the design has one canonical rich-interaction path instead of two competing blocking channels
- persistence, timeout, and non-goals are concrete enough for implementation

No further document changes needed from my side.

Vote: APPROVE
