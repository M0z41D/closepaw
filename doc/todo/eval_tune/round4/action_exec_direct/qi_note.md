我现在的click, longpress, scroll, swipe这些action经常execution有问题,就是它execute并且return说它execute succeeded,但是实际上没有执行,就是UI没有发生该有的变化。我现在每次都是执行一个agent task任务,然后任务中间可能会调用到这几个action,然后我observe它action前后的变化,但是这样太慢了。(参考doc/todo/eval_tune/round2/click/align/design，doc/todo/eval_tune/round3/swipe/align/design/claude_plan.md，这些implement完了，tool execution执行过程中还是错误百出）

我现在想先不管我的agent的大脑,这个LLM思考怎么样,我要先让我的这些action在execution这个层面保证正确稳定。我想先debug把这一部分搞定,这就需要我有一个很短的链路,在每一层,在任何一个场景下,我可以直接call某一个action,然后就直接让我的这个tool execution engine来执行这个action(不会跳转到android agent主界面，然后输入一个task来执行，而是直接在当前屏幕状态下，直接执行某个tool,deterministically执行),然后我可以通过adb前后截图,来看这个action是不是执行成功。同时呢,我可以看直接call adb的action,比如adb的tap等等(.reference/mobile_agents都是用adb来模拟用户操作的),这样来对比在相同场景下,我的toolexecution和它这个adb模拟的结果是否一样,因为adb的一般很稳定,但是我现在的基于accessibility的API的不太稳定。你帮我设计一个tech design能让我来快速的做这个debug,来让我的底层tool execution变得bug free。