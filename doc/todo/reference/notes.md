

1. Two-layer architecture is common, while some adds more agent on top of it.
    - Droidrun uses Manager-Executor.
    - AutoDev uses Planner-Executor.
    - Mobile Agent v3 uses Manager-Executor with ActionReflector and NoteTaker.
    - minitap uses Planner-Executor with Orchestrator, Contextor, Cortex and Summarizer.
2. Memory is common, is organized in different formats. And note taking is done by a particular agent in their above designed agent system.
    - Droidrun uses ..., note taking is done [when]
    - AutoDev uses ..., note taking is done [when]
    - Mobile Agent v3 uses ..., note taking is done [when]
    - minitap uses ..., note taking is done [when]
3. todo list or subgoals are common, while their formats can also vary, which usually is beared by the planner/manager.
    - Droidrun uses ..., todo list is done [when]
    - AutoDev uses ..., todo list is done [when]
    - Mobile Agent v3 uses ..., todo list is done [when]
    - minitap uses ..., todo list is done [when]
4. in terms of state management, and what contexts are shared across agents.
    - Droidrun ...
    - AutoDev ...
    - Mobile Agent v3 has a shared state management layer (InfoPool), and xxx is shared, while yyy are agent-specific.
    - minitap ...
5. the planner agents' output goal, granularity and formats are different
    - Droidrun planner uses ...
    - AutoDev planner uses a specific high-level set of tools, many has a 1-to-1 mapping to the executor agents' actions, but instead of element/pixel, it focus on the intent, and leave the grounding to the executor.
    - Mobile Agent v3 planner uses ...
    - minitap planner uses ...
6. subgoal updates and replanning triggering can vary.
    - Droidrun ...
    - AutoDev ...
    - Mobile Agent v3 ...
    - minitap ...